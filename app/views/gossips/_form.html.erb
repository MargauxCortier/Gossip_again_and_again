<%= form_with(model: gossip, local: true) do |form| %>
  <% if gossip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gossip.errors.count, "error") %> prohibited this gossip from being saved:</h2>

      <ul>
      <% gossip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <div class="field">
      <%= form.label :'Author :' %>
      <%= form.text_field :anonymous_gossiper %>
    </div>

    <div class="field">
      <%= form.label :'Content :' %>
      <%= form.text_field :content %>
    </div>

    <div class="actions">
      <%= form.submit class: "btn btn-outline-info btn-sm"%>
    </div>

<% end %>
